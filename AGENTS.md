# AGENTS.md

このリポジトリで作業する Codex / AI エージェント向けルールです。

## 目的

モバイルオーダーAPIの変更時に、実装だけでなくテスト・ドキュメントも必ず同期させ、
インターン生が安心して学習できる状態を維持すること。

## 必須ルール

1. **APIを変更したらテストを更新/追加すること**
   - 対象: `backend/tests/test_api.py`
   - エンドポイント追加・仕様変更・エラーハンドリング変更時は、
     正常系/異常系の少なくとも片方を更新する。

2. **APIを変更したら学習用ドキュメントを更新すること**
   - 対象:
     - `backend/README.md`（使い方・起動方法・実行方法）
     - `backend/TEST_CASES.md`（テストケース一覧）
   - 仕様変更により既存記述が古くなる場合は、必ず修正する。
   - 新規API追加時は、必要に応じてテストケースを新規追記する。

3. **変更の確認コマンドを実行すること**
   - 最低限:
     - `python3 -m py_compile backend/app/*.py backend/tests/test_api.py`
   - Dockerが使える環境では推奨:
     - `docker compose run --rm test`

4. **PRメッセージに明記すること**
   - API変更時は以下を必ず記載:
     - 何を変更したか
     - どのテストを更新/追加したか
     - どのドキュメントを更新したか

## 補足

- 学習用バックエンドなので、過度な抽象化より可読性を優先する。
- エラー形式 `{"message": "..."}` の統一を壊さないこと。
- `cart_items` / `orders` / `order_items` の責務分離を維持すること。
